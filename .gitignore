# ── Instance Config (real instance data — never commit) ──
config/company-instance.yaml
config/opportunity.yaml
config/opportunity-discovery-rules.yaml
config/product-lines.yaml
config/opportunity-watchlist.yaml
config/platform-profiles.yaml
config/company-context.yaml
config/opportunity-source-queries.yaml
config/opportunity-source-feeds.yaml

# ── Sensitive / Env ──
.env
.env.*
*.env.local
*.env.*

# ── Runtime Data ──
.ai-company-os/
data/
logs/

# ── Reports (generated content, may contain real business info) ──
reports/work-order-drafts/
reports/workflow-counter.json
reports/run-ledger/

# ── Build Artifacts ──
__pycache__/
*.pyc
node_modules/
.next/
backend/docs/evidence/

# ── Cloudflare / Wrangler ──
.wrangler/

# ── OS ──
.DS_Store
Thumbs.db

# ── Local Machine Config ──
config/launchd/

# ── Personal Business Data (DO NOT COMMIT) ──
research/

# ── Core Business IP (proprietary modules, scoring rules, real evaluations) ──
private/

# v0.46 — Company Memory Core Lite (private operational data only)
private/memory/candidates/**
private/memory/approved/**
private/memory/rejected/**
private/memory/context-packs/generated/**
