# IMPLEMENTATION_NOTES

1. 本包是高质量骨架，不是填完密钥就直接生产可跑的最终配置。
2. 需要根据你本机 OpenClaw 版本确认字段兼容性。
3. 建议先最小化验证：只启 CEO + 一个项目主管 + 一个后台 Agent。
4. Feishu 先只暴露 CEO 与各项目主管；后台 Agent 走内部调用。
5. 默认保守权限：不要一开始就给外部前台 Agent shell 与文件写权限。
6. 短剧项目的 ChatGPT 主创部分保留为人工协同，不强行塞进自动调用。
